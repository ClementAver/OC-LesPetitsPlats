class Search {
  constructor(recipes) {
    this._recipes = recipes;
    this._searchBar = "";
    this._ingredients = [];
    this._appliances = [];
    this._utensils = [];
  }

  search() {
    // le tri avec la search bar
    instanciatedRecipes.forEach((instanciatedRecipe) => sortedRecipes.push(instanciatedRecipe));
  }
}
